//集合
为什么要有集合:可以自动扩容,数组只可以存储基本数据类型和引用数据类型.集合可以存储引用数据类型
但是不可以存储基本数据类型,可以将基本数据类型变成其对应的包装类.数组长苏是固定的,集合长度是可
变得.自动伸缩,可长可短.
//集合的学习:
arrylist
<E>泛型
构造和成员方法:(可以总结为四个字:增删改查)
boolean add(E e) 添加元素,返回值表示是否添加成功
boolean remove(E e) 删除指定元素,返回值表示对否删除成功
E remove(int index) 删除指定索引的元素,返回被删除的元素
E set(int index,E e)修改指定元素索引下的元素,返回原来的数值
E get(int index) 获取指定索引的元素
int size()集合的长度,也就是集合中元素的个数

